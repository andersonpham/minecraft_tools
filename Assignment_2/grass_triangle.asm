.orig x3000
getp
REG
LD R4, Z_DIST
REG
LD R3, BLOCK_GRASS
REG
BRZ Z_DIST_ZERO

FIRST_LOOP
AND R5, R5, #0
ADD R5, R4, R4
ADD R5, R5, #1
reg
ADD R2,R2,R4
LOOP
JSR PLACE_BLOCK
BRP LOOP
ADD R0,R0,#1
ADD R2,R2,R4
ADD R2,R2,#1
ADD R4,R4,#-1
REG
BRP FIRST_LOOP

Z_DIST_ZERO
SETB

HALT

PLACE_BLOCK
SETB
ADD R2, R2, #-1
ADD R5,R5,#-1
reg
RET

Z_DIST .FILL #1
BLOCK_GRASS .fill #2
.END
